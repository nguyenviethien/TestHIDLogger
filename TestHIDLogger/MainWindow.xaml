<Window x:Class="TestHIDLogger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestHIDLogger"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid Margin="10" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" Text="VID (hex):"/>
            <TextBox x:Name="VidBox" Width="80" Text="0x1234" />
            <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Text="PID (hex):"/>
            <TextBox x:Name="PidBox" Width="80" Text="0x5678" />

            <Button x:Name="BtnOpen" Width="100" Margin="12,0,0,0" Content="Open" Click="BtnOpen_Click"/>
            <Button x:Name="BtnClose" Width="100" Margin="6,0,0,0" Content="Close" Click="BtnClose_Click" IsEnabled="False"/>
            <CheckBox x:Name="AutoScrollChk" Margin="16,0,0,0" Content="Auto scroll" IsChecked="True"/>
            <Button x:Name="BtnClear" Width="80" Margin="16,0,0,0" Content="Clear" Click="BtnClear_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="LogBox"
                     FontFamily="Consolas"
                     FontSize="12"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
            />
        </ScrollViewer>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Idle"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="CountText" Text="Packets: 0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
